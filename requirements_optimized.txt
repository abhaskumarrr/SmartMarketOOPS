# Optimized Requirements for M2 MacBook Air 8GB RAM
# Only essential packages for core functionality

# Core Web Framework (minimal)
fastapi>=0.95.0
uvicorn[standard]>=0.22.0

# Essential Data Processing (lightweight)
pandas>=1.5.0
numpy>=1.23.0

# Configuration
python-dotenv>=1.0.0

# HTTP Requests (lightweight)
httpx>=0.24.0

# Real-time Data & Trading
ccxt>=4.0.0
websockets>=11.0.0

# Technical Analysis (lightweight)
ta>=0.10.2

# Optional: Basic ML (comment out if not needed)
# scikit-learn>=1.2.0
