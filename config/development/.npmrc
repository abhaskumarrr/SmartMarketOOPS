# Suppress deprecated package warnings for packages we can't easily replace
audit-level=moderate
fund=false
legacy-peer-deps=false

# Engine strict mode to enforce Node.js version requirements
engine-strict=true

# Prefer exact versions for better reproducibility
save-exact=false
save-prefix=^

# Reduce noise from deprecated packages we can't control
loglevel=warn
